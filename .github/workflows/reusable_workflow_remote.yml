on:
  workflow_call:
    inputs:
      input_name:
        description: "Input name"
        required: true
        type: string
      topic_name:
        description: "Topic name"
        required: true
        type: string
      
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      - run: echo "This is an input ${{ inputs.input_name }}."
      - run: echo "This is an topic ${{ inputs.topic_name }}."
      - run: |
          if [[ ${{ inputs.topic_name }} == 'dc-health' ]]; then
            echo "Secrete for dc-health ${{ secrets.MY_SECRET }}"
          if else [[ ${{ inputs.topic_name }} == 'iia' ]]; then
            echo "Secrete for iia ${{ secrets.MY_CALLED_SECRET }}"
          else
            echo "Secrete for other ${{ secrets.MY_CALLED_SECRET }}"
          fi
      
